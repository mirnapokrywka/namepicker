var codeTalkNames = [];

function resetNames(){
	codeTalkNames = ['Bernice', 'Elliette', 'Mirna', 'Erinn', 'Aimee', 'Yadira', 'Janene', 'Denise'];

}

function namePicker(){
	var namePick = codeTalkNames.splice(Math.floor(Math.random() * codeTalkNames.length), 1);
}
function namePicker(){
	var namePick = codeTalkNames.splice(Math.floor(Math.random() * codeTalkNames.length), 1);
}
